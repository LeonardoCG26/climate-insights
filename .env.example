# Server-side OpenWeather API key. NEVER exposed to the browser: it is used
# only by the /api/openweather proxy (serverless in production, Vite middleware
# in dev). Do not use the VITE_ prefix — that would inline it into the bundle.
OPENWEATHER_API_KEY=your_openweather_api_key
